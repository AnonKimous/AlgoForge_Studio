display_name: Algorithm Development
short_description: Build, mount, validate, and profile algorithm packages from source manifests.
default_prompt: Use the algorithm development skill to inspect the package catalog, build an algorithm, mount it, validate its protocol, and inspect its performance.
