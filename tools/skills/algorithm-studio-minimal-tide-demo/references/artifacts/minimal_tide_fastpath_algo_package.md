# Minimal Tide Fast Path `.algo` Package

Expected source bundle folder:

- `algorithmLib/algorithmSrc/minimal_tide_fastpath/`

Expected runtime bundle folder after build:

- `algorithmLib/algorithmruntimeLib/minimal_tide_fastpath/`

Expected `.algo` package path:

- `algorithmLib/algorithmruntimeLib/minimal_tide_fastpath/minimal_tide_fastpath.algo`

What to tell the user:

- the package is now buildable
- the source files live under `algorithmLib/algorithmSrc/minimal_tide_fastpath/`
- the runtime package lives under `algorithmLib/algorithmruntimeLib/minimal_tide_fastpath/`
- `debugTool` should now be able to discover and inspect `minimal_tide_fastpath`
