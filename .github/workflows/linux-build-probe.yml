name: Linux Build And Teapot Acceptance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Acceptance workflow is temporarily paused while the selected-algorithm configure scope is fixed."
