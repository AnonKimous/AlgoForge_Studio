# devAlgo

This folder is the central landing zone for development algorithm project files.

Use it for:

- the default `algoDevDoc` template
- sample project packages
- other editor-facing seed projects that should be shared by the UI and agent flow
